name: Build TunnelFlare

on:
  push:
    branches: [ main, windows-compatibility ]
  workflow_dispatch:

jobs:
  build-linux:
    name: Build Linux AppImage
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libfuse2 file desktop-file-utils

    - name: Setup AppImage Tool
      run: |
        wget https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-x86_64.AppImage
        chmod +x appimagetool-x86_64.AppImage

    - name: Prepare AppDir (Niess Python Base)
      run: |
        wget https://github.com/niess/python-appimage/releases/download/python3.9/python3.9.25-cp39-cp39-manylinux2014_x86_64.AppImage -O python.AppImage
        chmod +x python.AppImage
        ./python.AppImage --appimage-extract
        mv squashfs-root AppDir
        rm python.AppImage

    - name: Install Application
      run: |
        PYTHON=./AppDir/opt/python3.9/bin/python3.9
        PIP=./AppDir/opt/python3.9/bin/pip3.9
        $PYTHON -m pip install --upgrade pip
        $PIP install --force-reinstall --no-cache-dir .

    - name: Configure AppDir
      run: |
        find AppDir -name "*.desktop" -delete
        find AppDir -name "*.xml" -delete
        find AppDir -name "*.metainfo" -delete
        cp -f tunnelflare_gui/resources/tray.svg AppDir/tunnelflare.svg
        ln -sf tunnelflare.svg AppDir/.DirIcon
        cat > AppDir/tunnelflare.desktop <<EOF
[Desktop Entry]
Type=Application
Name=TunnelFlare
Exec=tunnelflare
Icon=tunnelflare
Categories=Network;Development;
Comment=A modern GUI for Cloudflare Tunnels
Terminal=false
EOF
        find AppDir -name AppRun -delete
        cat > AppDir/AppRun <<'EOF'
#!/bin/bash
HERE=$(dirname "$(readlink -f "$0")")
export PYTHONHOME="$HERE/opt/python3.9"
export PYTHONPATH="$HERE/opt/python3.9/lib/python3.9/site-packages"
export PATH="$HERE/opt/python3.9/bin:$PATH"
export LD_LIBRARY_PATH="$HERE/opt/python3.9/lib:$HERE/opt/python3.9/lib/python3.9/site-packages/PySide6/Qt/lib:$LD_LIBRARY_PATH"
export QT_PLUGIN_PATH="$HERE/opt/python3.9/lib/python3.9/site-packages/PySide6/Qt/plugins"
export QT_X11_NO_MITSHM=1
exec "$HERE/opt/python3.9/bin/python3.9" -m main "$@"
EOF
        chmod +x AppDir/AppRun

    - name: Package AppImage
      env:
        ARCH: x86_64
      run: ./appimagetool-x86_64.AppImage AppDir TunnelFlare-x86_64.AppImage

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TunnelFlare-Linux
        path: TunnelFlare-x86_64.AppImage

  build-windows:
    name: Build Windows EXE
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller psutil PySide6 qrcode Pillow
        pip install .

    - name: Build with PyInstaller
      run: |
        pyinstaller --noconsole --onefile --name TunnelFlare --add-data "tunnelflare_gui/resources;tunnelflare_gui/resources" main.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: TunnelFlare-Windows
        path: dist/TunnelFlare.exe
